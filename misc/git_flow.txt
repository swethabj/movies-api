# Make a project folder
mkdir movies-api && cd movies-api

# Initialize git
git init

# Create go module (use repo path)
go mod init github.com/swethabj/movies-api

# Create files / folders -- project structure 


# First Commit & Push
git add .
git commit -m "Initial commit: Movies API project setup"

#create branch
git branch -M main_branch_1

# Link to remote repo
git remote add origin https://github.com/swethabj/movies-api.git

# Push to GitHub
git push -u origin main_branch_1