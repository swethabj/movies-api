#.env.example is a template file that lists all the environment variables your app needs.
#It does not contain real secrets but provides a reference for developers.
#Developers copy it to .env and then fill in actual values.
# Why?
#Prevents accidentally committing secrets (like DB passwords) to Git.
#Makes onboarding new developers easier (they just run cp .env.example .env).
#Ensures consistency across environments (dev, staging, prod).



DB_USER=your_user # Use a dedicated, least-privileged user.
DB_PASS=your_password # Use a strong, unique password. Never hardcode in code — always keep it in .env.
DB_HOST=127.0.0.1 #In production → might be an internal server (db.prod.local) or cloud DB (aws-rds-endpoint).
DB_PORT=3306
DB_NAME=movies_db
APP_PORT=8080 #API runs on 8080
LOG_LEVEL=info #Controls logging verbosity. Common levels: debug, info, warn, error.
#debug: Very detailed (used during development).
#info: General app flow (used in production).
#error: Only errors (for minimal logs).






# Notes
#Never commit .env to Git. Only commit .env.example.

# IN case of Multiple DB 
# Movies database
MOVIES_DB_USER=your_user
MOVIES_DB_PASS=your_password
MOVIES_DB_HOST=127.0.0.1
MOVIES_DB_PORT=3306
MOVIES_DB_NAME=movies_db

# Users database
USERS_DB_USER=your_user
USERS_DB_PASS=your_password
USERS_DB_HOST=127.0.0.1
USERS_DB_PORT=3306
USERS_DB_NAME=users_db

#user URL's to Connect if not individual variables 
MOVIES_DB_URL=mysql://your_user:your_password@127.0.0.1:3306/movies_db
USERS_DB_URL=mysql://your_user:your_password@127.0.0.1:3306/users_db